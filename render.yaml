services:
  - type: web
    name: consulta-inventario
    runtime: docker
    autoDeploy: true
    envVars:
      - key: ADMIN_PASSWORD
        sync: false
      - key: DATABASE_URL
        sync: false
