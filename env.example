services:
  - type: web
    name: consulta-inventario
    runtime: docker
    plan: free
    autoDeploy: true
    envVars:
      - key: ADMIN_PASSWORD
        sync: false
      - key: DATA_DIR
        value: /app/data
